FROM python:3.12-slim
WORKDIR /app
RUN pip install --no-cache-dir "fastapi>=0.115" "uvicorn[standard]>=0.32" "sqlalchemy>=2.0" "psycopg[binary]>=3.2"
COPY log_viewer /app/log_viewer
ENV PYTHONUNBUFFERED=1
CMD uvicorn log_viewer.main:app --host 0.0.0.0 --port 8080
